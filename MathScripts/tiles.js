// sample inputs
var Lenght = 20;
var Width = 30;
var box = 12;
var area = 600;

// calculations
var boxes = area / box
var totalBoxes = (boxes * 10) / 100 + boxes
// output
console.log("Total boxes is " + boxes);
console.log("total boxes with 10% " + totalBoxes)