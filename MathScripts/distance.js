// sample inputs
var x1 = 30;
var x2 = 20;
var y1 = 15;
var y2 = 5;

// calculations

var distance = Math.sqrt(Math.pow(x2-x1 , 2) + Math.pow(y2-y1 , 2));

// output
console.log("Total distance is " + distance);